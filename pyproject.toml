[project]
name = "snowfl"
version = "0.4.0"
description = "An unofficial Python wrapper for the Snowfl API, enabling programmatic torrent searches."
readme = "README.md"
requires-python = ">=3.14.7"
dependencies = [
    "certifi>=2025.11.12",
    "charset-normalizer>=3.5.1",
    "idna>=3.20",
    "requests>=2.34.2",
    "urllib3>=2.8.0",
]

[project.optional-dependencies]
dev = ["build==1.6.1", "coverage==7.16.1", "pytest==9.1.1", "pytest-rerunfailures==15.1", "twine==6.2.0"]

[tool.pytest.ini_options]
markers = [
    "live: hits snowfl.com — skipped by default, run with -m live",
]
addopts = "-m 'not live'"
